# Images

All images in this folder are AI-generated or synthetic (real folder screenshots, but of a fake folder tree with fake show/shot codes) — no real production data, real footage, or real internal paths anywhere here.

**Remaining placeholder:**

- `PLACEHOLDER_folder_example_generic.jpeg` — a real screenshot, YL2's naming convention rather than this repo's, so it's unused here. Reads as generic/low-risk either way. Sitting here for possible reuse in `yule-log-delivery-pipeline` instead of being deleted.

**In active use, wired into the main README:**

- `synthetic_plate_burnin.png`, `synthetic_layer01.png`, `synthetic_layer02.png`, `synthetic_merged.png`, `synthetic_merged_clean.png` — AI-generated shot footage matching the real burn-in convention from the onboarding manual. Shot codes (`DMO_070_NGHT_Sh0080`, `DMO_090_ZOOM_Sh0450`) use a fictional film/scene naming, deliberately styled to read as a real show's actual shotcode rather than a template.
- `folder_scene_list.png`, `folder_reference_footage.png`, `folder_nuke_script.png`, `folder_plates_breakdown.png`, `folder_layer02_frames.png` — real Windows Explorer screenshots of a real (but entirely fake-content) folder tree built to match `directory_creator_config.yaml`'s exact schema, using the same `DMO_*` fictional show/shot codes as the footage images above. This is what replaced the three real production screenshots (real hostname, real scene codes) that used to sit here — the fake tree these were taken from lives outside this repo and isn't published.
